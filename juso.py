# id() : 변수 주소값 반환하는 함수

# a = 10
# b = a

# a is b : a와 b의 주소값이 같은지 다른지 True ,False로 반환
# 위에 저 변수들은 참이 나옴

# a = 10
# b = a[:] 이러면 이제 복사 한걸 넣기 때문에 각각 사용 ㄱㄴ

# from copy import copy
# b = copy(a) 요런식으로 모듈 사용할수도 있음

# b = a.copy() 라는 함수로 사용 ㄱㄴ


# a,b = b,a 로 하면 변수 하나 더 안 만들어도 둘의 값 바꾸기 삽 가능